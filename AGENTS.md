# 実装とレビューの原則

このリポジトリでは、動作の正しさに加えて、意図の伝わりやすさと将来の変更の安全性を重視する。
個別の書き方を暗記するのではなく、以下の原則を実装の選択とレビューに適用する。

## 判断の原則

### 人間が読むときの認知負荷を最小にする

コードは、人間が読んで理解する際の認知負荷を最小にするように設計する。
読み手が同時に覚えておく状態・条件・処理の詳細を減らし、意味のまとまりを名前で表現する。
関数を読むだけで目的と流れを把握でき、必要なときだけ詳細へ進める構造を目指す。

具体的な目安として、一つの関数に5文以上を書く場合は、意味論的に正しいまとまりを
別の関数として切り出せないか検討する。ここでの文はコードの文（statement）であり、行数ではない。
多くの場合、適切に意味を抽出して名前を付けることで、読み手が追う詳細を減らせる。
ただし、すべての関数を5文以内に収める制限ではなく、分割を検討するきっかけとして扱う。
抽出先に明確な役割を命名できるか、呼び出し元の抽象度が揃うかを確認する。
単に短くするための分割や、引数・関数間の行き来を増やして理解を難しくする分割は避ける。

### 意図を直接表現する

名前・型・API・処理のまとまりで、そのコードが何を担うかを表す。
読み手が低水準の処理から目的を推測しなくても理解できるかを確認する。
コメントはコードの逐語説明ではなく、判断の理由・制約・不変条件を補う。

### 確立された抽象化を優先する

一般的な問題には、言語・フレームワーク・分野で確立された仕組みを優先する。
独自に処理を書く前に、既存の仕組みが意味・制約・境界条件を引き受けられないか確認する。
採用時には依存追加や運用の負担も考慮し、全体の複雑さが減るかで判断する。
既存コードの書き方も無条件には踏襲せず、同じ基準で評価する。

### 責務と依存の境界を揃える

異なる理由で変更される処理を区別し、環境依存の判断を適切な境界へ集める。
本質的な処理が、実行環境やテストの都合に引きずられていないか確認する。
テストは本番と同じ主要処理を通し、必要な外部依存を境界で差し替える。

### 必要最小限の複雑さにする

クラス・引数・分岐・依存・抽象化には、現在の要件に基づく理由を持たせる。
将来の可能性だけを理由に汎用化せず、読む側と変更する側の負担を減らす。
短さや分割数など、一つの指標を満たすために全体の理解を難しくしない。

### 変更しやすさも検証する

動作が正しいことと、設計が保守しやすいことは別々に確認する。
責務に沿った変更が局所的に済むか、影響範囲と依存関係を追えるかを確認する。
抽象化の好みだけで、互換性・データ保全・既存の動作を損なわない。

## 作業の進め方

1. 実装前に、関連する仕様・呼び出し元・既存の仕組みを確認する。
   既存コードは参考例として扱い、上記の原則に照らして採否を判断する。
2. 実装後に差分を読み直し、認知負荷・意図・既存機能の再実装・責務・不要な複雑さ・変更の影響を見直す。
   5文以上の関数では、意味のまとまりの抽出によって理解しやすくできないか検討する。
   テストや静的解析の成功だけを、設計レビューの代わりにしない。
3. 変更に応じたテスト・静的解析で検証する。
   テストは実装の形をなぞるのではなく、守るべき動作や失敗時の性質を確認する。
4. 指摘を受けたら、個別の修正に加えて、その背後にある判断の問題を捉える。
   同じ問題が現在の変更範囲にもないか確認し、関連する箇所を一貫して修正する。
   無関係な箇所への大規模な変更には広げない。
5. 継続して役立つ学びは既存の原則へ統合する。
   個別事例の禁止事項を増やすのではなく、適用条件と判断理由を明確にする。

原則同士が衝突する場合は、正しさとデータ保全を守ったうえで、全体の複雑さが小さい案を選ぶ。
重要なトレードオフは説明する。機械的に検証でき、繰り返し役立つ制約は、適切なlintやテストへ落とし込む。

## コミット

ユーザーの指示ごとに、検証が済んだ意味のある区切りでコミットする。
対象の差分を確認し、無関係な変更を含めない。コミット後は結果と検証内容を報告する。
