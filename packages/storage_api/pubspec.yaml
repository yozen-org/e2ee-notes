name: storage_api
description: "Minimal opaque object-store contract for E2EE applications."
version: 0.0.1
homepage:

environment:
  sdk: ^3.13.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
