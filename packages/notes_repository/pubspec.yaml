name: notes_repository
description: Encrypted operation repository for e2ee-notes.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.13.0

dependencies:
  e2ee_core:
    path: ../e2ee_core
  storage_api:
    path: ../storage_api

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
